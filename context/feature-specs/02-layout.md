we have to design, the files or arrange the build files by features do you understadn what i mean, dont let us jump, so we need the base components that frame every screen, the top nabar, the sidebars for large screens, the bottom navbar, these are all components that would be resused abd extended in every chapter that follows and pages.

so we woudl create the different components, 

so the requirements for theses componets first, 
e.g fixed- heignt top navabr
the middle section should have a banner card, followed by a tabs for the different markrt categories using the `/omponents/tab` followed by the individual market card ( this shoudl even be a component, or we can make it a place order for now adn defien the spec for this later in a different section or build plan), 


see lets define all the ui components first as build 1, so you list all the components out, define all of them from the market card, to the 


ourr ui context defines the flow, but we havent define how the whole ui would look like for moibile and all, we have to define the wireframe yes that is it, so we would build the wireframe which is mirrored from the ui, context flow, drawing and listing the approbaite components needed so we say, the requirements for each componets that makes up the wireframe, what each button uses, using the rules and the css set in the global.css file, 
example


so first we list, all components, pages and parent components that we need listed out, cause i dont think we have that yet, and every page

so the comones are buttons and all, pages ( we decided we would use dialogs or everything ryt to reduce stress and code time and rerneders) the only time we are using a page render is when you cahnge the market with the tabs component, taht is when the middle part in large screen directly under the tabs rerender do youunderstnad, the parnet componets are the mareket card, how the images are positioned, the buttons, the typogrpahy, the dialog ( not built ), so lets rewrite the build, after we have built the comoints or parent functions if you may, we then create ( individual spec files for each features) then we use the user-flow.md file to define how everything makes sense, and how everything links do you udnerstnad, or shoudl we pick off of the user flow and built off of each flow, so we write a wireframe.md file that we write out the wireframe for all pages and parnet componets, do you udnerstnad what i mean 